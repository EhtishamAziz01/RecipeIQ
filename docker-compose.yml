version: "3.8"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data/vectors:/app/data/vectors  # Persist vector store
    restart: unless-stopped
